pathListGenerator <- function(base_folder_path, years_list) {
  
  paths_list = list()
  
  for (year in years_list) {
    path = paste0(base_folder_path, year, ".xlsx")
    paths_list <- append(paths_list, path)
  }
  
  return(paths_list)
}

